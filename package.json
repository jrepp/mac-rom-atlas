{
  "name": "mac-rom-atlas",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "export:maps": "node tools/export_atlas_maps.js",
    "validate:data": "node scripts/validate_project_data.js",
    "validate": "npm run validate:data && npm --prefix services/mcp-server run build",
    "prepare:hooks": "git config core.hooksPath .githooks"
  }
}
