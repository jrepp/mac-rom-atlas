{
  "name": "mac-rom-atlas",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "export:maps": "node tools/export_atlas_maps.js",
    "validate:data": "node scripts/validate_project_data.js",
    "validate:service": "npm --prefix services/mcp-server run build",
    "validate": "npm run validate:data && npm run validate:service",
    "prepare:hooks": "git config core.hooksPath .githooks"
  }
}
